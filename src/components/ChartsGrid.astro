---
interface Props {
    tempChartId?: string;
    solarChartId?: string;
}

const { tempChartId = "chart-temp", solarChartId = "chart-solar" } = Astro.props;
---

<div class="my-8">
    <div class="flex items-center gap-3 mb-6">
        <div class="w-1 h-8 bg-[#7BAF6E] rounded-full"></div>
        <h2 class="font-['DM_Serif_Display',serif] text-2xl font-normal text-[#E8F0E4]">Data Visualization</h2>
    </div>
    <div class="grid grid-rows-1 lg:grid-rows-2 gap-6">
        <div class="bg-[#0B1D0F]/60 border border-[#2D5A3D]/30 p-5 rounded-2xl backdrop-blur-sm">
            <h3 class="text-base font-semibold text-[#D4A843] mb-4 flex items-center gap-2 tracking-wide">
                <span>🌡️</span> Thermal Profile
            </h3>
            <canvas id={tempChartId} class="w-full rounded-xl border border-[#2D5A3D]/20"></canvas>
        </div>
        <div class="bg-[#0B1D0F]/60 border border-[#2D5A3D]/30 p-5 rounded-2xl backdrop-blur-sm">
            <h3 class="text-base font-semibold text-[#D4A843] mb-4 flex items-center gap-2 tracking-wide">
                <span>☀️</span> Solar Potential
            </h3>
            <canvas id={solarChartId} class="w-full rounded-xl border border-[#2D5A3D]/20"></canvas>
        </div>
    </div>
</div>
