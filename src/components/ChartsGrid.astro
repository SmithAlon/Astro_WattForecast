---
interface Props {
    tempChartId?: string;
    solarChartId?: string;
}

const { tempChartId = "chart-temp", solarChartId = "chart-solar" } = Astro.props;
---

<div class="my-8">
    <h2 class="text-2xl font-bold text-gray-800 mb-5">📈 Data Visualization</h2>
    <div class="grid grid-rows-1 lg:grid-rows-2 gap-8">
        <div class="bg-gray-50 p-5 rounded-2xl shadow-md">
            <h3 class="text-lg font-semibold text-gray-700 mb-4 flex items-center gap-2">
                <span>🌡️</span> Thermal Profile
            </h3>
            <img id={tempChartId} alt="Temperature chart" class="w-full rounded-lg">
        </div>
        <div class="bg-gray-50 p-5 rounded-2xl shadow-md">
            <h3 class="text-lg font-semibold text-gray-700 mb-4 flex items-center gap-2">
                <span>☀️</span> Solar Potential
            </h3>
            <img id={solarChartId} alt="Solar chart" class="w-full rounded-lg">
        </div>
    </div>
</div>
