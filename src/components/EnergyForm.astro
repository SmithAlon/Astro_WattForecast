---
import LocationSearch from './LocationSearch.astro';

interface Props {
    defaultDays?: number;
    minDays?: number;
    maxDays?: number;
}

const { defaultDays = 30, minDays = 7, maxDays = 365 } = Astro.props;
---

<div class="bg-gray-50 p-8 rounded-2xl mb-8">
    <form id="form">
        <div class="grid grid-cols-1 md:grid-cols-3 gap-6 mb-6">
            <div class="flex flex-col">
                <label class="font-semibold mb-2 text-gray-700 flex items-center gap-2">
                    <span>👤</span> User Type
                </label>
                <select 
                    id="user_type"
                    class="py-3 px-4 text-base border-2 border-gray-200 rounded-lg transition-all focus:outline-none focus:border-indigo-500 focus:ring-2 focus:ring-indigo-100"
                >
                    <option value="home">🏠 Home</option>
                    <option value="industry">🏭 Industry</option>
                </select>
            </div>
            
            <LocationSearch />
            
            <div class="flex flex-col">
                <label class="font-semibold mb-2 text-gray-700 flex items-center gap-2">
                    <span>📅</span> Days to Project
                </label>
                <input 
                    type="number" 
                    id="days" 
                    value={defaultDays} 
                    min={minDays} 
                    max={maxDays}
                    class="py-3 px-4 text-base border-2 border-gray-200 rounded-lg transition-all focus:outline-none focus:border-indigo-500 focus:ring-2 focus:ring-indigo-100"
                >
                <small class="text-gray-500 mt-1">Between {minDays} and {maxDays} days</small>
            </div>
        </div>
        
        <div class="flex flex-col md:flex-row gap-4">
            <button 
                type="submit" 
                class="flex-1 py-4 px-8 text-base font-semibold border-none rounded-xl cursor-pointer transition-all flex items-center justify-center gap-2 bg-gradient-to-br from-indigo-500 to-purple-600 text-white hover:-translate-y-0.5 hover:shadow-xl disabled:opacity-50 disabled:cursor-not-allowed disabled:transform-none"
            >
                <span>🔍</span> Analyze & Generate Suggestion
            </button>
            <button 
                type="button" 
                id="btnExport"
                class="flex-1 py-4 px-8 text-base font-semibold border-none rounded-xl cursor-pointer transition-all flex items-center justify-center gap-2 bg-green-500 text-white hover:bg-green-600 hover:-translate-y-0.5 hover:shadow-xl disabled:opacity-50 disabled:cursor-not-allowed disabled:transform-none"
            >
                <span>📥</span> Export CSV Data
            </button>
        </div>
    </form>
</div>
