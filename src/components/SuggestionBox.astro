---
interface Props {
    title?: string;
    id?: string;
}

const { title = "AI-Personalized Suggestion", id = "suggestion" } = Astro.props;
---

<div class="bg-gradient-to-br from-pink-400 to-rose-500 text-white p-8 rounded-2xl my-8 shadow-xl">
    <h3 class="text-2xl font-bold mb-5 flex items-center gap-3">
        <span>💡</span> {title}
    </h3>
    <div id={id} class="bg-white/20 p-5 rounded-xl whitespace-pre-wrap leading-relaxed backdrop-blur-sm"></div>
</div>
