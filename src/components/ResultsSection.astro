---
import MetricsGrid from './MetricsGrid.astro';
import SuggestionBox from './SuggestionBox.astro';
import ChartsGrid from './ChartsGrid.astro';

interface Props {
    id?: string;
}

const { id = "results" } = Astro.props;
---

<div id={id} class="results hidden">
    <h2 class="text-2xl font-bold text-gray-800 mb-5">📊 Climate Analysis</h2>
    <MetricsGrid />
    <ChartsGrid />
    <SuggestionBox />
</div>
</style>
